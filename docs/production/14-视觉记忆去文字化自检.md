# 视觉记忆去文字化：修改与自检

日期：2026-09-05。题库版本：`1.6-prepilot-visual-20260905`。

五项元能力、十五项二级能力、30个任务、65次作答、评分及映射规则未调整。本轮不再主观加难。

## 已修改

| 项目 | 现在的学生端 |
| --- | --- |
| 视觉位置板 | 3列×2行的6个SVG图形，不显示编号、名称或分组提示 |
| 语义材料 | 只显示虚构对象名称及属性，SC、SD等编号留在内部字段 |
| M01 | 3道图形识别题，12个SVG选项；不显示图形名称 |
| M05 | 先显示目标SVG，再选择位置；目标与开场材料使用同一图形组件 |
| M06 | 保持符号图形选项；呈现→S06→R06→M06流程不变 |
| 图片加载 | 未加载或加载失败时锁定选项和提交；点阵加载完成后才开始1800毫秒展示 |
| 学生页用时 | “预计15～18分钟”，仅为预估范围，未宣称实测完成 |
| 03 | “我的优势在哪些学科更容易发挥”，简化学科需求与优势标签 |
| 04 | “我的优势可以用在哪些学习任务”，补充实际学习先后还需结合掌握情况和目标分数 |
| 05 | “看懂每门课应该怎么学，再把自己的优势用进去。” |
| 表达与证据字段 | 学生页面使用“表达辨析”；不显示“准确表达”“证据充分”“证据不足”等内部字段 |

正文依照 stop-slop 检查，把抽象术语改成具体学习动作；保留必要的结果解释边界。报告不折叠、不删减学科内容。

## 自动检查

- 21个测试文件、105项测试通过。
- M01每题4个不同图形，且只有正确选项与位置板材料完全匹配。
- M05三个目标分别对应原板左下、中下、右下，即时与延迟子集不混用。
- 检查实际DOM文本与无障碍标签，确认记忆页、M01、M05没有VA/VB/SC/SD编号或图形名称。
- 模拟慢加载、加载失败：图形未就绪不能作答；点阵不会在等待加载时消耗观看时间，也不直接计错。
- 原有任务各满分1分、计时不计分、材料不重播、点阵中断换图、覆盖及难度分布测试继续通过。
- TypeScript编译、单HTML打包与独立文件静态检查通过。审计生成器导出同一SVG组件，并将实际图形插入完整题库审计表。

## 浏览器流程检查

使用已安装的Playwright Chromium测试本机开发预览 `http://127.0.0.1:5173/`。Browser插件及browser技能未提供，未新增依赖。手机模拟390×844；报告补查360×800和桌面1280×900。

流程：填写信息→确认说明→材料与30个任务→完成→完整报告→刷新恢复。使用正式材料时长24/24/18/12秒，无记忆加速。

- 页面非空，未出现框架错误覆盖层；pageerror为0。
- 完成30个任务，4组记忆材料各呈现一次。
- 中途刷新保留作答；报告刷新保留版本、完成数、实际用时和生成时间。
- M01与M05可见内容中未发现名称和编号；图形不依赖系统字体。
- 抽查数轴、旋转、拼合、位置变换图片加载成功，所查手机页面无横向溢出。
- 学生报告出现新标题和学习优先顺序边界，不显示内部证据字段。

程序自动选择选项所得完成用时不是学生完成时间，不能用来证明15～18分钟估计。

## 仍需人工预测试

1. iPhone Safari、安卓Chrome、微信内置浏览器真机测试；断网刷新、后台切换、系统字体放大和横竖屏切换未在真实手机验证。
2. 总体中位用时、P90、每任务用时、示例耗时和反复阅读的页面。
3. 语义六组24秒、同时保持4项与5项序列的负荷。
4. 去掉图形名称不能阻止学生自行命名或默念；通过访谈记录策略，不能据此宣称测得纯视觉记忆或排除了语言影响。
5. 现有优势阈值、题目区分度、备用点阵等值性和报告映射仍未经过样本验证；本轮不修改阈值。

本地联调HTML仍含内部题库和答案，编号可以在内部源码/审计中存在，但不向学生呈现。正式部署仍须移除客户端答案并使用服务端判分。

新材料版本使用独立进度存储，避免旧版带名称材料的作答混入；旧版本地记录未删除。

## 截图

[视觉位置板](/private/tmp/meta-visual-material-1.png) · [M01图形选项](/private/tmp/meta-visual-mobile-task-0.png) · [M05目标与位置](/private/tmp/meta-visual-mobile-task-24.png) · [报告03](/private/tmp/meta-visual-report-03.png)
